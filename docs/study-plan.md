# План подготовки

Ядро — 124 карточки из 511 под роль LLM / NLP / AI Engineer,
плюс 23 карточки фоном — на случай вопросов из смежных областей.
Остальное в банке — справочник, а не программа.

## Как работать

1. **Сначала произнеси вслух, потом открывай эталон.** Прочитать разбор и подумать
   «ну да, я это знал» — самый надёжный способ обмануть себя. Не смог сказать связно
   за 60 секунд — значит не знаешь.
2. **Механику выводи, а не заучивай.** Всё, что помечено как «вывести на бумаге»,
   должно получаться с нуля. Выведенное не забывается, заученное рассыпается на
   первом уточняющем вопросе.
3. **Спорь с эталоном, а не подгоняйся под него.** Банк — список тем, которые обязаны
   прозвучать, а не единственно верная формулировка. Если ты сказал по делу, но иначе,
   это не ошибка. Если формулировка банка кажется прямо неверной — сверься с
   первоисточником дня, а не с тренажёром.
4. **Суждения привязывай к своим проектам.** На вопросах вида «когда RAG, а когда
   дообучение» правильный ответ звучит как «в моём проекте было так, я выбрал это,
   потому что…». Поэтому `profile/projects.md` важнее половины теории.
5. **Сомневаешься в карточке — иди в первоисточник.** Банк написан языковой моделью
   и не верифицирован; в каждом дне указано, где смотреть правду.

## Ритм дня — примерно час

| Блок | Время | Что делать |
|---|---|---|
| Повторение | 15 мин | Карточки с телефона по вчерашним и позавчерашним темам |
| Тренажёр | 20 мин | 3 вопроса дня через `run.py` вслух, с диктофоном |
| Глубина | 15 мин | Первоисточник дня + вывод на бумаге |
| Проект | 10 мин | Один блок в `profile/projects.md` |

Пятый день каждой недели — без новых карточек: только повторение и мок.

## Неделя 1 — LLM: как это работает и как это обслуживается

### День 1 — Внимание и блок трансформера

| id | Вопрос |
|---|---|
| `attn` | Какая сложность у self-attention и что именно ускоряет FlashAttention? |
| `mha` | Зачем внимание разбивают на несколько голов, если можно посчитать одну большой? |
| `gqa` | Что такое GQA и MQA, какую проблему они решают и чем платят? |
| `ffn` | Зачем в блоке трансформера нужен FFN и почему в нём расширение в 4 раза? |
| `norm` | LayerNorm против RMSNorm и pre-norm против post-norm — что и почему выбирают сейчас? |
| `dl_positional` | Зачем трансформеру позиционное кодирование? |
| `encdec` | Encoder-only, decoder-only и encoder-decoder — когда что выбирают? |

**Первоисточник:** Attention Is All You Need; d2l.ai, глава про внимание

**Сделать руками:** Вывести scaled dot-product на бумаге и объяснить деление на корень из d_k

```bash
python run.py --id attn
```

### День 2 — Инференс: как генерируется токен

| id | Вопрос |
|---|---|
| `kv` | Зачем нужен KV-cache и что он ломает при батчинге? |
| `prefill` | Чем prefill отличается от decode и почему их считают отдельно? |
| `batching` | Что такое continuous batching и почему обычный статический батч плох для LLM? |
| `llm_throughput` | Как устроен размен между задержкой и пропускной способностью и что крутить под каждую цель? |
| `spec` | Как работает speculative decoding и почему качество не падает? |
| `sampling` | Как работают temperature, top-k и top-p и что выбрать под извлечение фактов? |
| `llm_repetition` | Модель зациклилась и повторяет одну фразу. Почему это происходит и как лечить? |

**Первоисточник:** Документация vLLM (continuous batching, PagedAttention); блог о speculative decoding

**Сделать руками:** Посчитать размер KV-cache для 7B при контексте 8k и батче 16

```bash
python run.py --id kv
```

### День 3 — Память, стоимость, размер модели

| id | Вопрос |
|---|---|
| `quant` | Чем отличаются GPTQ, AWQ и GGUF-кванты и что теряется при 4 битах? |
| `llm_kvquant` | Можно ли квантизовать KV-cache и чем за это платят? |
| `tokcost` | Как оценить стоимость и память для инференса 7B модели? |
| `llm_p99` | Почему у LLM-сервиса p99 задержки в разы хуже среднего и что с этим делать? |
| `llm_tp` | Модель не помещается в одну карту. Какие есть способы её распределить? |
| `llm_sizechoice` | Как выбрать размер модели под задачу и не переплачивать? |
| `struct` | Как гарантировать, что модель вернёт валидный JSON по схеме? |

**Первоисточник:** QLoRA (NF4); документация GPTQ/AWQ; llama.cpp про GGUF

**Сделать руками:** Прикинуть, сколько GPU нужно на 200 rps при 2k контекста и 200 токенов ответа

```bash
python run.py --id quant
```

### День 4 — Длинный контекст и позиции

| id | Вопрос |
|---|---|
| `len` | Модель обучена на 4k контекста — что произойдёт на 32k и как это чинят? |
| `contextrot` | Что такое lost in the middle и как это учитывать при сборке промпта? |
| `llm_ctxstage` | Как модели дают длинный контекст и почему не обучают сразу на 128k? |
| `rope` | RoPE, ALiBi и обучаемые позиционные эмбеддинги — в чём разница и что даёт экстраполяцию на длину? |
| `llm_prefixcache` | Что такое кэширование префикса и как под него проектировать промпт? |
| `llm_memory` | Как вести долгий диалог, когда история не помещается в контекст? |
| `llm_vocab` | На что влияет размер словаря токенизатора? |

**Первоисточник:** RoFormer (RoPE); Position Interpolation, YaRN; Lost in the Middle

**Сделать руками:** Объяснить, почему модель на 4k ломается на 32k и что делает интерполяция позиций

```bash
python run.py --id len
```

### День 5 — Повтор недели без новых карточек

**Первоисточник:** —

**Сделать руками:** Прогнать через run.py 8 случайных вопросов из пройденного, вслух и на диктофон

## Неделя 2 — RAG, эмбеддинги, оценка

### День 1 — Поиск: основа RAG

| id | Вопрос |
|---|---|
| `chunk` | Почему наивный чанкинг по N символов ломает RAG и что с этим делают? |
| `hybrid` | Зачем в RAG совмещают BM25 и векторный поиск? |
| `rerank` | Зачем нужен реранкер, если есть эмбеддинги? |
| `index` | Flat, IVF и HNSW — чем отличаются и когда что брать? |
| `qrw` | Что такое query rewriting и когда без него RAG не работает? |
| `nl_bm25` | Чем BM25 отличается от TF-IDF и почему лексический поиск не вытеснили эмбеддинги? |

**Первоисточник:** BM25 (Robertson & Zaragoza); документация FAISS и HNSW

**Сделать руками:** Нарисовать полный путь запроса от текста до ответа с этапами и задержками

```bash
python run.py --id chunk
```

### День 2 — RAG в проде

| id | Вопрос |
|---|---|
| `ground` | Как измерить, что ответ RAG опирается на контекст, а не выдуман? |
| `llm_ragdiag` | Ответ RAG-системы плохой. Как понять, виноват поиск или генерация? |
| `llm_ragk` | Сколько фрагментов класть в контекст и почему больше не значит лучше? |
| `llm_ragfresh` | Как поддерживать индекс актуальным и что происходит при удалении документа? |
| `llm_multihop` | Вопрос требует объединить сведения из двух разных документов. Почему обычный RAG не справляется? |
| `llm_ragtables` | В базе знаний много PDF с таблицами и схемами. Что ломается в стандартном RAG? |

**Первоисточник:** Оригинальная статья RAG (Lewis et al.); материалы по RAGAS

**Сделать руками:** Придумать, как отделить ошибку поиска от ошибки генерации на своих данных

```bash
python run.py --id ground
```

### День 3 — Эмбеддинги

| id | Вопрос |
|---|---|
| `emb` | Как обучают модель эмбеддингов и зачем нужны hard negatives? |
| `pool` | CLS, mean pooling или last token — как получают вектор предложения? |
| `matry` | Что такое Matryoshka-эмбеддинги и зачем они на практике? |
| `embtrain` | Почему при обучении эмбеддингов так важен размер батча? |
| `llm_embchoice` | Как выбрать модель эмбеддингов под свой поиск? |
| `llm_embreindex` | Вы меняете модель эмбеддингов. Что происходит с существующим индексом? |

**Первоисточник:** Sentence-BERT; Matryoshka Representation Learning; карточки моделей bge/e5

**Сделать руками:** Объяснить, зачем нужны hard negatives и почему важен размер батча

```bash
python run.py --id emb
```

### День 4 — Оценка генерации

| id | Вопрос |
|---|---|
| `evalgen` | Как оценивать качество генерации, когда эталонных ответов нет? |
| `halluc` | Почему модель галлюцинирует и какие меры реально работают? |
| `llm_judgebias` | Модель судит ответы другой модели. Какие смещения у такого судьи и как их гасить? |
| `eval_ab` | Как собрать офлайн-набор для проверки регрессий в LLM-продукте? |
| `contam` | Что такое контаминация бенчмарков и как её обнаруживают? |
| `llm_offon` | Офлайн-метрики выросли, а пользователям стало хуже. Где расхождение? |
| `ppl` | Что показывает perplexity и почему по ней нельзя сравнивать разные модели? |

**Первоисточник:** Judging LLM-as-a-Judge (MT-Bench); материалы по контаминации бенчмарков

**Сделать руками:** Составить план офлайн-набора для своей задачи: что в него войдёт и почему

```bash
python run.py --id evalgen
```

### День 5 — Повтор + первый мок

**Первоисточник:** —

**Сделать руками:** Взять 3 вопроса из недели 1 и 3 из недели 2, отвечать без подглядывания

## Неделя 3 — Дообучение, алайнмент, фундамент DL

### День 1 — PEFT

| id | Вопрос |
|---|---|
| `lora` | Что именно обучается в LoRA и за что отвечают r и alpha? |
| `qlora` | Чем QLoRA отличается от LoRA и что такое NF4 и двойное квантование? |
| `peftcompare` | LoRA, prefix tuning, prompt tuning, IA³ — чем отличаются и что выбрать? |
| `llm_multilora` | В проде нужно много дообученных вариантов одной модели. Как это обслуживать? |
| `lossmask` | Почему при SFT лосс считают только по токенам ответа? |
| `packing` | Что такое packing при SFT и какую ошибку легко допустить с маской? |

**Первоисточник:** LoRA; QLoRA; документация PEFT от HuggingFace

**Сделать руками:** Объяснить, что именно обучается в LoRA и почему B инициализируется нулями

```bash
python run.py --id lora
```

### День 2 — Данные для дообучения

| id | Вопрос |
|---|---|
| `sftdata` | Что важнее при SFT — объём данных или их качество, и как это проверяют? |
| `forget` | Почему при дообучении на домене модель теряет прежние умения и как это гасят? |
| `llm_sftsize` | Сколько примеров нужно для дообучения под задачу и что важнее — объём или качество? |
| `distill` | Что такое дистилляция и почему обучение на мягких вероятностях лучше, чем на метках? |
| `llm_synthdata` | Можно ли обучать модель на данных, сгенерированных другой моделью? |
| `llm_ragvsft` | Когда задачу решает RAG, когда дообучение, а когда только промпт? |

**Первоисточник:** LIMA (качество против объёма); Self-Instruct

**Сделать руками:** Сформулировать критерий, когда задача решается RAG, а когда дообучением

```bash
python run.py --id sftdata
```

### День 3 — Алайнмент и рассуждения

| id | Вопрос |
|---|---|
| `dpo` | Чем DPO отличается от RLHF через PPO? |
| `rewardhack` | Что такое reward hacking в RLHF и как его ловят? |
| `llm_prefpairs` | Как собирают данные предпочтений и что чаще всего портит такой датасет? |
| `llm_rlvr` | Чем обучение с проверяемой наградой отличается от RLHF на предпочтениях? |
| `llm_reasoning` | Что дают рассуждающие модели с длинной цепочкой мыслей и чем за это платят? |
| `cot` | Почему chain-of-thought улучшает результат и когда он вредит? |
| `selfcons` | Что такое self-consistency и когда её стоит применять? |

**Первоисточник:** InstructGPT (RLHF); DPO; Chain-of-Thought; Self-Consistency

**Сделать руками:** Вывести, из чего складывается лосс DPO и что делает beta

```bash
python run.py --id dpo
```

### День 4 — Фундамент DL

| id | Вопрос |
|---|---|
| `dl_backprop` | Объясните backpropagation — что именно происходит на обратном проходе? |
| `dl_vanish` | Почему градиенты затухают и какие приёмы это решают? |
| `dl_optimizers` | SGD с моментом против Adam — когда что работает лучше? |
| `dl_weightdecay` | Чем weight decay отличается от L2-регуляризации и почему это важно для Adam? |
| `dl_normwhy` | Почему нормализация ускоряет обучение и что она делает на самом деле? |
| `dl_losschoice` | Почему для классификации берут кросс-энтропию, а не MSE? |
| `dl_softmaxnum` | Почему функции потерь принимают логиты, а не вероятности от softmax? |

**Первоисточник:** Decoupled Weight Decay (AdamW); How Does Batch Normalization Help Optimization; d2l.ai

**Сделать руками:** Объяснить, почему кросс-энтропия принимает логиты, а не вероятности

```bash
python run.py --id dl_backprop
```

### День 5 — Практика обучения

| id | Вопрос |
|---|---|
| `dl_debug` | Модель не учится — лосс стоит на месте. Какой порядок действий? |
| `dl_lrfind` | Как быстро подобрать learning rate? |
| `dl_gradclip` | Зачем нужен gradient clipping и как выбрать порог? |
| `dl_gradaccum` | Что такое градиентное накопление и чем оно отличается от настоящего большого батча? |
| `dl_paramcount` | Как оценить память, нужную для обучения модели? |
| `dl_activation_ckpt` | Как gradient checkpointing экономит память и когда его включать? |
| `dl_seed` | Насколько результат зависит от сида и что с этим делать при сравнении моделей? |

**Первоисточник:** Karpathy, A Recipe for Training Neural Networks

**Сделать руками:** Посчитать память под обучение 7B в bf16 с Adam и с checkpointing

```bash
python run.py --id dl_debug
```

## Неделя 4 — Прод, статистика, системный дизайн, HR

### День 1 — MLOps: наблюдаемость

| id | Вопрос |
|---|---|
| `psi` | Как отличить дрейф данных от дрейфа целевой зависимости и чем их меряют? |
| `repro` | Что нужно зафиксировать, чтобы результат обучения воспроизводился? |
| `op_fs` | Зачем нужен feature store и что такое train-serve skew? |
| `op_monitor` | Что логировать в проде, чтобы потом можно было разобраться в инциденте? |
| `op_registry` | Что должен хранить реестр моделей и почему версии файла с весами недостаточно? |
| `op_shadowdata` | Как проверить, что признаки в проде считаются так же, как при обучении? |
| `op_incident` | Модель в проде начала выдавать мусор. Порядок действий? |

**Первоисточник:** Chip Huyen, Designing Machine Learning Systems, главы про мониторинг и дрейф

**Сделать руками:** Описать, что мониторить в своём последнем проекте и с какими порогами

```bash
python run.py --id psi
```

### День 2 — MLOps: выкатка и эксплуатация

| id | Вопрос |
|---|---|
| `op_canary` | Как безопасно выкатывать новую версию модели? |
| `op_shadow` | Что даёт теневой запуск модели и чего он принципиально не покажет? |
| `op_rollback` | Что нужно, чтобы откат модели занимал минуты, а не дни? |
| `op_slo` | Что делать сервису, когда модель не ответила вовремя или упала? |
| `op_cost` | Инференс стал слишком дорогим — какие рычаги есть и в каком порядке их применять? |
| `op_thresholds` | Модель не менялась, а доля срабатываний выросла вдвое. Что произошло? |
| `op_modelinput_log` | Что и как логировать при каждом предсказании? |

**Первоисточник:** Та же книга, главы про деплой; Google SRE Book (алерты и SLO)

**Сделать руками:** Написать план отката для модели, которая уже работает у тебя в проде

```bash
python run.py --id op_canary
```

### День 3 — Статистика и A/B

| id | Вопрос |
|---|---|
| `st_pval` | Что означает p-value и какие три интерпретации ошибочны? |
| `st_power` | Ошибки первого и второго рода, мощность — как они связаны и чем управляются? |
| `st_mde` | Как рассчитать размер выборки для A/B и что такое MDE? |
| `st_peek` | Почему нельзя останавливать A/B-тест, как только появилась значимость? |
| `st_mult` | Проверяем 20 метрик в A/B — в чём проблема и как её решают? |
| `st_cuped` | Что такое CUPED и за счёт чего он ускоряет эксперименты? |
| `st_ratio` | Метрика — отношение (конверсия на сессию, средний чек). Почему обычный t-тест по ней врёт и что делать? |
| `st_srm` | Что такое SRM и почему при его срабатывании результат теста нельзя интерпретировать? |

**Первоисточник:** Kohavi, Tang, Xu — Trustworthy Online Controlled Experiments (главы 1-5, 17-22)

**Сделать руками:** Посчитать размер выборки для прироста конверсии на 1% при базе 5%

```bash
python run.py --id st_pval
```

### День 4 — Системный дизайн

| id | Вопрос |
|---|---|
| `sd_rag200` | RAG по внутренней документации, 200 rps, ответ за секунду — как устроена система и где узкие места? |
| `sd_domain` | Руководство хочет дообучить LLM на данных банка. Как понять, нужно ли это, и как спланировать? |
| `agent` | Спроектируй ассистента по внутренней документации банка на 500 сотрудников — из чего он состоит? |
| `latency` | Модель должна отвечать за 200 мс при 100 rps — как рассуждать о бюджете? |
| `tooluse` | Как заставить модель надёжно вызывать инструменты? |
| `sd_support` | Спроектируйте бота поддержки, который может ошибиться, но не должен навредить. Что закладываете? |
| `llm_agentfail` | Почему многошаговые агенты разваливаются на длинных задачах и что с этим делают? |
| `llm_toolschema` | Как описывать инструменты, чтобы модель вызывала их правильно? |

**Первоисточник:** Alex Xu, Machine Learning System Design Interview; посты о проде LLM-агентов

**Сделать руками:** Спроектировать RAG на 200 rps вслух за 20 минут, с бюджетом задержки по этапам

```bash
python run.py --id sd_rag200
```

### День 5 — HR и питч проекта

| id | Вопрос |
|---|---|
| `tellme` | Расскажите о себе за две минуты |
| `whyleave` | Почему вы уходите с текущего места? |
| `hr_salary` | Какие у вас зарплатные ожидания? |
| `hr_expectations` | Как обсуждать зарплату, если вилка не названа? |
| `hr_notech` | Как объяснить сложный проект человеку без технического бэкграунда? |
| `hr_failure` | Расскажите про свою самую дорогую ошибку. |
| `hr_questions` | У вас есть вопросы к нам? |
| `hr_pitch_project` | Расскажите про свой проект за три минуты. |

**Первоисточник:** —

**Сделать руками:** Записать на диктофон рассказ о себе за 2 минуты и про проект за 3 минуты

```bash
python run.py --id tellme
```

## Фон — читать, но не зубрить

### Классический ML — по диагонали

| id | Вопрос |
|---|---|
| `ml_biasvar` | Разложение на смещение и дисперсию — что это и как проявляется на практике? |
| `gbdt` | Почему на табличных данных градиентный бустинг обычно бьёт нейросети? |
| `ml_rfgb` | Random Forest и градиентный бустинг — чем принципиально отличаются? |
| `calib` | Что такое калибровка вероятностей и почему высокая ROC-AUC не означает, что модель калибрована? |
| `ml_rocpr` | Когда ROC-AUC вводит в заблуждение и надо смотреть PR-кривую? |
| `leak` | Что такое утечка признака и как её обнаружить до продакшна? |
| `groupkf` | Когда обычный KFold даёт завышенную оценку и что ставят вместо него? |
| `imbal` | Дисбаланс классов один к тысяче — что делать и чего не делать? |
| `ml_thresh` | Модель выдаёт вероятность, а бизнесу нужно решение — как выбирают порог? |
| `ml_erroranalysis` | Как проводить разбор ошибок модели и что это даёт? |

### Computer Vision — если спросят

| id | Вопрос |
|---|---|
| `cv_conv` | Что такое рецептивное поле и как его увеличивают без роста числа параметров? |
| `cv_normchoice` | BatchNorm, GroupNorm, LayerNorm — как выбирать нормализацию в CV-задачах? |
| `cv_det` | Two-stage и one-stage детекторы — в чём разница и что выбрать? |
| `cv_map` | Как считается mAP и что означает порог IoU в этой метрике? |
| `cv_seg` | Чем отличаются semantic, instance и panoptic сегментация и какие метрики у каждой? |
| `cv_transfer` | Как правильно делать transfer learning в CV и что размораживать? |
| `cv_vit` | ViT против CNN — какие индуктивные смещения теряются и чем это компенсируют? |

### NLP-классика — если спросят

| id | Вопрос |
|---|---|
| `tok` | Как работает BPE и почему токенизаторы плохи для казахского и других агглютинативных языков? |
| `nl_tokcomp` | BPE, WordPiece и Unigram — чем отличаются алгоритмы построения словаря? |
| `ner` | Как устроена разметка BIO в NER и где она ломается? |
| `nl_tfidf` | Как устроен TF-IDF и почему он до сих пор конкурентоспособен? |
| `nl_annotate` | Как организовать разметку и как понять, что она достаточно качественная? |
| `nl_longdoc` | Нужно классифицировать документы на десятки страниц. Как обойти ограничение длины? |

## Как понять, что готов

Не по баллу тренажёра, а по трём признакам:

1. По любой карточке ядра ты говоришь 60 секунд связно, без пауз и без «ну как бы».
2. На вопрос «почему именно так» у тебя есть причина, а не ссылка на то, что так принято.
3. По каждому проекту из `profile/projects.md` ты называешь цифру: что было, что стало,
   как мерил. Именно здесь разваливается большинство кандидатов.

Если по трети карточек ядра ты не согласен с моей формулировкой пунктов — это нормально
и даже полезно: перепиши их через `--mine`, банк станет твоим.
